OPTION psf_version = 3.0;

BEGIN driver vectorblox_mxp

  OPTION supported_peripherals = (vectorblox_mxp);
  OPTION supported_os_types = (DTS);
  OPTION driver_state = ACTIVE;
  OPTION NAME = vectorblox_mxp;

END driver
